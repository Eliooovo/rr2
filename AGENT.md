# STM32 模块开发规范

本文件适用于本仓库中的 STM32 外设驱动、设备驱动、BSP 和功能模块开发。除非用户明确给出不同要求，开发和修改代码时必须遵守以下规则。

## 1. CubeMX 与生成代码边界

- `.ioc` 是 STM32 硬件与外设配置的唯一配置源。凡是 `.ioc`/CubeMX 能表达的配置，必须优先修改 `.ioc`，再调用 STM32CubeMX CLI 校验并重新生成代码。
- 时钟树、GPIO、DMA、NVIC、中断、外设工作模式、通信位时序、FIFO/Buffer 数量等可由 CubeMX 配置的内容，不得改用 HAL 初始化代码、寄存器操作或手改生成文件实现，否则后续重新生成会覆盖或产生冲突。
- 修改前必须核对当前 `.ioc`、CubeMX 版本、已生成代码和工作区差异，只调整任务需要的配置，不顺带格式化或更新无关选项。
- 修改 `.ioc` 后必须使用与工程版本一致或兼容的 STM32CubeMX CLI 执行 `config load` 和 `project generate`，随后检查 `.ioc` 与所有生成文件的差异并进行完整构建；不得只修改 `.ioc` 而不重新生成。
- CubeMX CLI 脚本应使用临时文件，典型流程为 `config load <project.ioc>`、`project generate`、`exit`。不得把临时脚本或无关生成产物留在仓库中。
- 不得手工修改 CubeMX 生成的初始化函数、时钟、GPIO、DMA、MSP、NVIC 等代码来修补生成结果。若生成结果异常，应修正 `.ioc` 后重新生成。
- 未经用户明确授权，除 `main.c` 和 `main.h` 外，禁止向任何 CubeMX 自动生成文件写入代码，即使写入位置位于 `USER CODE BEGIN/END` 保留区也不允许。对其他生成文件的改动必须先说明目标文件、写入内容和必要性并取得授权。
- 如果 CubeMX CLI 不可用、版本不兼容、配置校验失败或生成产生大量无关变化，应停止并向用户说明，不能退回到 HAL 或寄存器方式绕过。
- 只有 CubeMX 确实无法表达的运行时行为，才允许放在非生成的 BSP 或模块代码中通过 HAL API 实现；必须先确认不会与生成代码重复配置或冲突。除非用户明确授权，不得直接操作外设寄存器。
- CubeMX 重新生成后必须检查用户代码是否被保留。发现生成操作可能覆盖非 `USER CODE` 内容时，应先停止并说明风险。
- 验收标准是可重复生成：用户以后仅根据当前 `.ioc` 再次执行 CubeMX 代码生成时，所需硬件配置和模块行为不得丢失、改变或需要手工补回。自定义代码必须位于非生成文件或 CubeMX 明确保留的 `USER CODE` 区域。

## 2. 模块对象设计

- 一个硬件设备或逻辑实例应由一个结构体对象表示。
- 对象应按职责包含以下内容：
  - `config`：调用方在初始化前填写的配置。
  - `internal`：仅供模块内部维护的私有状态。
  - 公开状态：按合理颗粒度组织，可使用一个或多个嵌套结构体，例如 `state`、`feedback` 和 `fault`。
- 对象内存默认由调用方分配，模块不得擅自使用动态内存。
- 初始化函数默认只接收对象指针，并从对象自身的 `config` 读取配置。

## 3. API 与头文件

- 头文件只公开必要的枚举、状态码、结构体和操作函数。
- 私有辅助函数、协议中间状态和临时缓冲区应放在 `.c` 文件或对象的 `internal` 中。
- 配置、反馈、故障和运行状态应按用途分组，避免无意义的完全扁平化，也避免过深嵌套。
- 公开的物理量字段必须通过名称或注释注明单位，例如 `_rad`、`_rad_s`、`_nm`、`_c` 和 `_ms`。
- 除非协议明确要求，模块不得在用户不知情时自动保存参数、修改设备 ID、修改波特率或执行其他持久化操作。

## 5. 官方代码与协议移植

- 官方代码适合实例化和当前平台时，优先在外层封装调用，不随意修改其内部逻辑。
- 如果官方代码依赖固定宏、全局缓冲区、单例状态、特定芯片 HAL 或编译器行为，应先说明其不适合直接复用的原因。
- 经用户同意后，可以在自有模块内按照官方协议语义实现实例安全、平台适配的版本；不得宣称此类实现仍在直接调用官方 API。
- 不得照搬依赖编译器布局的位域、违反别名规则的类型转换或其他未定义行为。报文 ID 和字段应使用明确的掩码与移位构造。
- 协议层、传输层和外设配置层应保持清晰边界，避免协议代码直接控制时钟、GPIO 或中断。

## 6. HAL 回调与中断

- 实现 HAL 回调前，必须搜索工程中已有的强符号、弱符号和回调注册配置。
- 不得重复定义已经被工程覆盖的 HAL 全局回调，否则会造成链接冲突。
- 多个模块共享同一回调时，应使用统一分发器、HAL 回调注册机制或使用 BSP 层(若没有BSP层需和用户讨论是否需要创建)。
- 中断处理只执行必要的取帧、校验、解析和状态更新，不在中断中执行长延时、阻塞等待或复杂控制流程。
- 主循环与中断共享数据时，应根据访问方式正确使用原子操作、`volatile` 或临界区，并避免共享临时发送缓冲区引发竞争。

## 7. 代码完成后的用户交付说明

- 完成代码后，除说明修改内容和验证结果外，还必须提供用户可以直接执行的交付提示。
- 提供使用实际类型和 API 的最小示例，说明对象配置、初始化顺序、主要操作、数据或状态读取，以及需要调用的周期函数、回调或反初始化函数。
- 列出需要用户在外部设备、硬件或系统其他节点上核对的相关项目，例如地址或 ID、协议与数据格式、通信速率、工作模式、固件兼容性、接线和供电。只列与当前模块有关的内容。
- 将尚需用户处理的事项区分为“必须修改”和“需要核对”，并说明哪些已经由代码或配置确认、哪些尚未进行硬件验证。
