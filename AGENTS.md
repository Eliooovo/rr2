# STM32 开发规范

本规范适用于本仓库及其他 STM32 项目的模块、App 和调试工作；用户或具体项目的明确要求优先。

## 模块开发规范

### CubeMX 与生成边界

- `.ioc` 是硬件静态配置的唯一配置源。时钟、GPIO、DMA、NVIC、中断、外设模式与通信参数等，只要 CubeMX 能表达，就必须修改 `.ioc`，不得用 HAL 初始化、寄存器或手改生成代码替代。修改前核对 `.ioc`、CubeMX 版本、生成代码和工作区差异，只动任务相关配置。
- 修改 `.ioc` 后，用 CubeMX CLI 通过临时脚本执行 `config load`、`project generate`、`exit`，调用时临时脚本和 `.ioc` 均使用绝对路径；检查全部生成差异和用户代码保留情况，再完整构建。临时文件不得入库，最终工程必须能仅凭当前 `.ioc` 重复生成，无需手工补配置。
- 不得手改 CubeMX 生成的初始化、时钟、GPIO、DMA、MSP、NVIC 等代码。自定义代码放在非生成文件或明确保留区；除 `main.c`、`main.h` 外，未经用户明确授权不得修改任何生成文件，即使位于 `USER CODE` 区。确需修改时，先说明文件、内容和必要性。
- CubeMX 不可用、版本不兼容、校验失败、产生大量无关差异或可能覆盖非保留代码时，停止并说明，不得退回 HAL 或寄存器方式绕过。
- 只有 CubeMX 无法表达的运行时行为，才可在非生成的 BSP 或模块中通过 HAL API 实现，并避免重复配置；直接操作外设寄存器必须先获用户授权。

### 对象、API 与协议

- 一个设备或逻辑实例使用一个由调用方分配的结构体对象，不使用动态内存。对象按职责组织 `config`、私有 `internal` 和公开的 `state`/`feedback`/`fault`；初始化函数默认只接收对象指针并读取其 `config`。
- 头文件只公开必要类型和操作；私有函数、协议中间状态和缓冲区留在 `.c` 或 `internal`。状态按用途适度分组，公开物理量通过字段名或注释标明单位。
- 除非协议明确要求，不得暗中保存参数、修改设备 ID、波特率或执行其他持久化操作。
- 官方代码适配当前平台和多实例时优先封装复用。若其依赖固定宏、全局/单例状态、特定 HAL 或编译器行为，应先说明；经用户同意后可按官方协议语义自行实现，但不得称为仍在调用官方 API。
- 协议实现不得依赖位域布局、违规别名转换等未定义行为；报文使用明确的掩码和移位，协议、传输和外设配置保持分层。

### 回调、中断与交付

- 实现 HAL 回调前先搜索已有强/弱符号和回调注册配置，禁止重复定义全局回调。多模块共享时使用统一分发器、HAL 注册机制或 BSP；项目没有 BSP 时先与用户讨论是否创建。
- 中断只做必要的数据接收、校验、解析和状态更新，不阻塞。中断与主循环共享数据时正确使用原子操作、`volatile` 或临界区，并避免共享临时发送缓冲区竞争。
- 交付时说明修改和验证结果，并提供基于实际类型/API 的最小示例，覆盖配置、初始化、操作、状态读取、周期函数、回调及需要的反初始化。
- 只列当前模块相关的外部核对项，如 ID、协议格式、速率、模式、固件、接线和供电；将遗留事项分为“必须修改”和“需要核对”，区分代码已确认与尚未硬件验证的内容。

## App 开发规范

- 一个 App 负责一个完整功能，各 App 平级，不相互初始化。共享外设或数据由独立 App/BSP 单点拥有，其他 App 只通过最小只读接口访问。
- App 仅在自身 `.c` 中静态分配和配置所需模块/BSP 对象，不使用动态内存；没有模块的系统 App 只管理系统级运行时行为。GPIO、通信和中断等静态配置仍以 `.ioc` 为准。
- App 头文件默认只公开 `Init` 和 `RunPeriodic`，必要时增加只读状态或快照；不得公开内部对象、配置、控制辅助函数或可变全局状态。
- `main.c` 只调用 CubeMX 的 `MX_*_Init` 及各 App 的初始化、周期函数，不分配对象、填写配置或实现控制逻辑。
- 周期函数必须非阻塞，并在 App 内管理周期、离线超时、命令失效和安全零输出。中断数据经 BSP 统一分发；初始化失败且无法安全运行时进入统一错误处理，运行期按硬件反馈记录故障并进入安全状态。

## 调试规范

- 可按问题需要运行或暂停 CPU，并使用动态观察、断点、单步、调用栈、寄存器/变量读写、复位、烧录和脚本。当前已验证的 Agent 路线是 `arm-none-eabi-gdb` 配合 J-Link GDB Server，以及用于烧录、暂停读取和重复操作的 `JLinkExe` 脚本。
- 选择最容易确认问题且影响可控的方式。GDB Server、`JLinkExe` 和用户使用的 Ozone 通常不能同时独占同一 J-Link，应先断开占用者；Ozone 当前不作为 Agent 的命令行工具。未安装、未接入或未实际验证的方案只能作为建议，不能当作现有能力。
- 连接前核对芯片、探针序列号、接口、速率，以及板上固件与带 DWARF 信息的 ELF 是否匹配；重新构建或烧录后重新加载符号。调试操作已获允许，但仍须评估执行器和外设影响，避免无关的危险动作或持久化修改。
- 需要严格一致的数据时优先暂停读取。解释动态观察结果时，要考虑字段采样时刻不同；带 D-Cache 的 MCU 还可能出现后台读取与缓存不一致，不能把撕裂或陈旧数据直接判为固件故障。
- 优化后不可见的局部变量可用断点、反汇编或最小静态调试快照观察；快照不得成为 App 公共可变接口，也不得改变正常控制逻辑。
- 记录 ELF、调试方式、关键命令/脚本、观察值和结论，并说明是否发生 halt、reset、flash 或写入。暂停造成的通信、超时和时序异常必须与固件故障区分；Agent 若必须自动动态观察，应说明当前工具链尚未实现。
- 调试结束后恢复用户期望的运行状态、清除临时断点，并报告仍保留的临时代码或变量。
